<h1>Eat-Da-Burger</h1>

<ul>
  {{#each burgers}}
  	<li>
  	   <p>ID: {{this.id}}</p>
       <p>Burger: {{this.burger_name}}</p>
	   <button id= "devour">Devour</button>
  	</li>
  {{/each}}
</ul>

<form action="/" method="POST">
	<textarea type="text" name="burger"></textarea>

	<button type="submit">Submit</button>
</form>

<h3>Enter a burger you'd like to eat and then click the "Submit" button</h3>